name: project_milkotea
description: "Delivering happiness, one sip at a time"

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  
  flutter_bloc: ^9.1.1
  equatable: ^2.0.8
  bloc: ^9.2.0
  intl: ^0.20.2
  http: ^1.6.0

  # Crypt
  crypto: ^3.0.2

  # Firebase
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
  firebase_storage: ^13.0.6

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

